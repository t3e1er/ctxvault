# Rust formatting configuration (applied workspace-wide)
# See: https://rust-lang.github.io/rustfmt/

edition = "2021"
max_width = 100
tab_spaces = 4
use_small_heuristics = "Max"
reorder_imports = true
