name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # ──────────────────────────────────────────────────
  # 1. Gate: Full validation suite before building
  # ──────────────────────────────────────────────────
  validate:
    name: Pre-Release Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: releasebuild-validate

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy lints
        run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings

      - name: Test suite
        run: cargo test --workspace --all-features --locked

      - name: Documentation check
        env:
          RUSTDOCFLAGS: "-D warnings"
        run: cargo doc --workspace --all-features --no-deps --locked

      - name: License & advisory check
        uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check

  # ──────────────────────────────────────────────────
  # 2. Build & Package Across OS Platforms
  # ──────────────────────────────────────────────────
  build-release:
    name: Build (${{ matrix.target }})
    needs: [validate]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            binary_name: cxtvault.exe
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
            binary_name: cxtvault
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-22.04
            binary_name: cxtvault
            use_cross: true
          - target: aarch64-apple-darwin
            os: macos-latest
            binary_name: cxtvault

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross (for Linux ARM64)
        if: matrix.use_cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build release binary
        shell: bash
        run: |
          if [ "${{ matrix.use_cross }}" = "true" ]; then
            cross build --release --locked --target ${{ matrix.target }} --bin cxtvault
          else
            cargo build --release --locked --target ${{ matrix.target }} --bin cxtvault
          fi

      - name: Package artifacts (POSIX)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          STAGING="cxtvault-${{ github.ref_name }}-${{ matrix.target }}"
          mkdir -p "$STAGING"
          cp "target/${{ matrix.target }}/release/${{ matrix.binary_name }}" "$STAGING/"
          cp README.md LICENSE THIRD_PARTY_LICENSES.md "$STAGING/" 2>/dev/null || true
          cp -r examples "$STAGING/" 2>/dev/null || true
          tar -czvf "$STAGING.tar.gz" "$STAGING"
          echo "ASSET_NAME=$STAGING.tar.gz" >> $GITHUB_ENV

      - name: Package artifacts (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $staging = "cxtvault-${{ github.ref_name }}-${{ matrix.target }}"
          New-Item -ItemType Directory -Force -Path $staging | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/${{ matrix.binary_name }}" -Destination $staging
          if (Test-Path README.md) { Copy-Item README.md -Destination $staging }
          if (Test-Path LICENSE) { Copy-Item LICENSE -Destination $staging }
          if (Test-Path THIRD_PARTY_LICENSES.md) { Copy-Item THIRD_PARTY_LICENSES.md -Destination $staging }
          if (Test-Path examples) { Copy-Item -Recurse examples -Destination $staging }
          Compress-Archive -Path $staging -DestinationPath "$staging.zip" -Force
          echo "ASSET_NAME=$staging.zip" >> $env:GITHUB_ENV

      - name: Upload artifact for release job
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: ${{ env.ASSET_NAME }}

  # ──────────────────────────────────────────────────
  # 3. Publish to GitHub Releases (Step 4.1)
  # ──────────────────────────────────────────────────
  publish-release:
    name: Publish GitHub Release
    needs: [build-release]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Generate SHA256 checksums
        working-directory: release-assets
        run: |
          sha256sum * > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
          draft: false
          prerelease: false
